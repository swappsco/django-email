============
Installation
============

At the command line::

    $ easy_install django-email

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-email
    $ pip install django-email
